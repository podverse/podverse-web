const common = 'common'
const errorMessages = 'errorMessages'
const pages = 'pages'
const queryLabels = 'queryLabels'

export const i18n = {
  fileNames: {
    all: [common, errorMessages, pages, queryLabels],
    common,
    errorMessages,
    pages,
    queryLabels
  }
}
