const
  path = require('path'),
  {databaseName} = require('./src/config.js'),
  PATH = 'src/repositories/sequelize',

  dbConfig = {
    "database": databaseName,
    "dialect": "postgres",
    "protocol": "postgres",
    "host": "127.0.0.1",
    "port": "5432"
  };

module.exports = {
  "migrations-path": path.resolve(PATH, 'migrations'),
  "seeders-path": path.resolve(PATH, 'seeders'),
  "models-path": path.resolve(PATH, 'models'),
  "config": __filename,
  "development": dbConfig,
  "production": dbConfig
}
